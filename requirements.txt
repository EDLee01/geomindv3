# GeoMind 4.0 Dependencies

# ── 核心框架 ──
chainlit>=2.0.0

# ── HTTP 客户端 ──
httpx>=0.25.0

# ── Qdrant 向量数据库 ──
qdrant-client>=1.7.0

# ── Embedding（文献检索，二选一） ──
fastembed>=0.3.0          # 推荐：轻量，不依赖 torch
# sentence-transformers   # 备选：更完整但更重

# ── 数据处理 ──
pandas>=2.0.0
openpyxl>=3.1.0           # Excel 支持

# ── 可视化（代码执行环境） ──
matplotlib>=3.7.0
numpy>=1.24.0

# ── 推荐：科学计算（代码执行时会自动安装缺失的库） ──
seaborn>=0.12.0           # 高级图表
scipy>=1.10.0             # 科学计算
scikit-learn>=1.3.0       # 机器学习

# ── PDF 处理 ──
PyMuPDF>=1.23.0           # PDF 文本/表格/图片提取
pdfplumber>=0.10.0        # PDF 表格提取（备选）

# ── 可选：地学扩展 ──
# geopandas>=0.14.0       # 地学矢量数据（需要 GDAL）
